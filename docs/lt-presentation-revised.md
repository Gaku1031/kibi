---
marp: true
theme: default
paginate: true
backgroundColor: #fff
backgroundImage: url('https://marp.app/assets/hero-background.svg')
style: |
  section {
    font-size: 28px;
  }
  h1 {
    color: #0066cc;
  }
  code {
    background: #f4f4f4;
    padding: 2px 6px;
    border-radius: 3px;
  }
  pre {
    background: #2d2d2d;
    color: #f8f8f8;
  }
---

# AWS Comprehendで作る<br/>感情分析日記アプリ
## 非同期処理設計とコスト最適化の検討

---

## 自己紹介 & プロジェクト概要

**kibi** - 感情の機微を発見する日記サービス

- 📝 日記を書くと自動で感情分析
- 🎨 8感情を三角形アイコンで可視化
- 💰 **同期→非同期設計で月$40削減を目指す**

**スタック**: Next.js (Amplify) + Hono (Lambda) + DynamoDB + Comprehend

---

## アジェンダ

### 1. アーキテクチャ概要
- 全体構成とAWSサービスの選定理由

### 2. Comprehend非同期処理の設計
- 8感情モデルとコスト最適化（**同期→非同期**）

### 3. 実装のポイント
- Hono on Lambda、感情アイコン生成アルゴリズム

### 4. ハマったポイント3選
- API 404、Lambda統合、CORS地獄

### 5. 技術選定の学び
- 調査→設計→実装のプロセス

---

## アーキテクチャ全体像

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│  Amplify    │────▶│ HTTP API     │────▶│   Lambda    │
│  (Next.js)  │     │ Gateway v2   │     │   (Hono)    │
└─────────────┘     └──────────────┘     └─────────────┘
                                                 │
                    ┌────────────────────────────┼────────┐
                    │                            │        │
              ┌─────▼──────┐            ┌───────▼─────┐  │
              │  DynamoDB  │            │ Comprehend  │  │
              │   Tables   │            │  Async Job  │  │
              └────────────┘            └─────────────┘  │
                                                         │
                                                    ┌────▼────┐
                                                    │   S3    │
                                                    │ Bucket  │
                                                    └─────────┘
```

---

## Comprehend: 同期 vs 非同期の設計比較

### 従来の同期API設計
```typescript
// ❌ エンドポイント必須 → 常時稼働で月$40
const command = new ClassifyDocumentCommand({
  EndpointArn: ENDPOINT_ARN,  // 😱 高い！
  Text: translatedText
});
```

### 設計した非同期ジョブ
```typescript
// ✅ エンドポイント不要 → pay-per-use
const command = new StartDocumentClassificationJobCommand({
  DocumentClassifierArn: CLASSIFIER_ARN,  // 分類器直接指定
  InputDataConfig: { S3Uri: `s3://bucket/input.txt` },
  OutputDataConfig: { S3Uri: `s3://bucket/output/` }
});
```

**想定コスト削減: 月$40 → $0（実行時のみ課金）**

---

## 非同期処理の実装設計

### バックエンドAPI設計
```typescript
// 1. ジョブ開始 → jobIdを返す
POST /diary/:id/analyze → { jobId, status: 'SUBMITTED' }

// 2. ポーリングでステータス確認（3秒間隔）
GET /diary/:id/analyze/status/:jobId
  → { status: 'IN_PROGRESS', progress: 50 }
  → { status: 'COMPLETED', diary: {...} }
```

### フロントエンド実装
```typescript
const jobId = await startAsyncAnalysis(diaryId);
// 3秒間隔でポーリング
await pollAnalysisStatus(diaryId, jobId);
```

**プログレスバーでUX改善**

---

## Comprehendカスタム分類器の準備

### Plutchikの感情の輪（8基本感情）
```
     Joy (喜び)
    /   |   \
Trust  |  Anticipation (期待)
       |
Fear - + - Anger (怒り)
       |
Sadness| Disgust (嫌悪)
    \  |  /
   Surprise (驚き)
```

### トレーニングデータ生成
```javascript
// 各感情150サンプル生成（合計1200サンプル）
const emotions = {
  joy: ["I am so happy today!", ...],
  trust: ["I have complete confidence...", ...],
  // ... 8感情分
};
```

**実装済み**: 学習データ生成スクリプト完成

---

## なぜHono on Lambda?

### 従来の課題
- API Gateway + Lambda統合: エンドポイントごとにLambda関数
- コールドスタート対策が大変
- ルーティングロジックの分散

### Honoを採用
```typescript
// backend/src/index.ts
import { Hono } from "hono";
import { handle } from "hono/aws-lambda";

const app = new Hono();
app.route("/diary", diaryRoutes);

export const handler = handle(app);  // これだけ！
```

**1つのLambda関数でRESTful API全体を管理**

---

## 感情アイコン生成アルゴリズム

### コンセプト
8つの感情をカラフルな三角形で表現

### 実装済みのポイント
```typescript
// 1. 感情スコアでサイズ決定
const size = Math.max(0.2, Math.min(1.0, value * 0.8 + 0.2));

// 2. 重複回避アルゴリズム（最小距離チェック）
const minDistance = (size + 0.3) * 30;
for (const pos of usedPositions) {
  const distance = Math.sqrt(
    Math.pow((x - pos.x) * 5, 2) + Math.pow((y - pos.y) * 5, 2)
  );
  if (distance < minDistance) return null; // 重複
}

// 3. シード値で再現性確保
const random = new SeededRandom(iconSeed);
```

---

## ハマったポイント 1: API 404エラー

### 問題
フロントエンドからAPIを呼ぶと404エラー

### 原因
```typescript
// ❌ 相対パスを使っていた
const API_BASE_URL = '/api';
// Amplify: https://xxx.amplifyapp.com/api → 存在しない
```

### 解決
```typescript
// ✅ 環境変数で動的設定
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL ||
  (process.env.NODE_ENV === 'production' ? '/' : 'http://localhost:8080');
```

**インフラとフロントエンドの連携が重要**

---

## ハマったポイント 2: Lambda統合の試行錯誤

### 最初: Lambda Web Adapter
```dockerfile
# ❌ HTTPサーバー起動が必要 → 面倒
COPY --from=public.ecr.aws/awsguru/aws-lambda-adapter:0.7.2 /lambda-adapter /opt/extensions/lambda-adapter
ENV AWS_LAMBDA_EXEC_WRAPPER=/opt/bootstrap
```

### 最終: hono/aws-lambda
```typescript
// ✅ シンプル！
import { handle } from "hono/aws-lambda";
export const handler = handle(app);
```

**公式サポートのアダプタを選ぶべき**

---

## ハマったポイント 3: CORS地獄

### 問題
CORSエラーが解消されない

### 原因と解決
```typescript
// 1. Hono側でCORSミドルウェア
app.use("*", cors({ origin: "*", ... }));

// 2. HTTP API Gateway v2でCORS設定
corsPreflight: {
  allowOrigins: ['*'],
  allowMethods: [CorsHttpMethod.GET, CorsHttpMethod.POST, ...],
}

// 3. API URL末尾のスラッシュ問題
API_URL="${API_URL%/}"  // 末尾スラッシュ削除
```

**HTTP API Gateway v2 + Hono + URL整形の三位一体**

---

## 技術選定の進め方（個人的な学び）

### ❌ NG: いきなり実装開始
「日記アプリ作って」→ よくある実装で終わる

### ✅ OK: 調査→設計→実装
1. **徹底調査**: Comprehendのドキュメント、料金体系
2. **アーキテクチャ設計**: 同期vs非同期の比較検討
3. **仕様明確化**: 8感情、非同期処理、プログレスバー
4. **段階的実装**: 基本機能→感情分析→最適化

**「何を作るか」「どう作るか」を分けて考える**

---

## 現在の実装状況

### ✅ 完成済み
- 日記CRUD機能（Hono + DynamoDB）
- 感情アイコン生成アルゴリズム
- 非同期処理API設計・実装
- 学習データ生成スクリプト（150サンプル/感情）

### 🚧 進行中
- Comprehendカスタム分類器の学習
- フロントエンドのポーリング機能
- プログレスバー実装

### 📋 今後の予定
- 本番環境でのコスト検証
- リアルタイム通知（EventBridge + WebSocket）

---

## GitHub Actions CI/CD

```yaml
jobs:
  deploy-backend:
    - Checkout code
    - Configure AWS credentials (OIDC)
    - Bootstrap CDK
    - Deploy infrastructure (CDK)
    - Get API URL from CloudFormation Output
```

**フロントエンドはAmplify Hostingで自動デプロイ**

### 分離デプロイのメリット
- バックエンド: GitHub Actions（インフラ管理）
- フロントエンド: Amplify（Next.js最適化）

---

## 想定コスト構成（本番環境）

| サービス | 同期API | 非同期ジョブ | 備考 |
|---------|--------|------------|------|
| Amplify Hosting | ~$5 | ~$5 | SSR込み |
| Lambda | ~$1 | ~$1 | リクエスト数少 |
| DynamoDB | ~$1 | ~$1 | オンデマンド |
| HTTP API Gateway | ~$1 | ~$1 | v2使用 |
| **Comprehend** | **$40** | **$0** | **エンドポイント vs ジョブ** |
| S3 | ~$0.5 | ~$0.5 | 一時ファイル |
| **合計** | **$48.5/月** | **$8.5/月** | **$40削減** |

**非同期化による大幅コスト削減を期待**

---

## まとめ

### 技術選定のポイント
✅ **調査重視**: AWSサービスの料金体系を理解
✅ **設計先行**: 同期vs非同期を比較検討
✅ **段階実装**: 基本機能→最適化の順序

### 学び
- **コスト意識**がアーキテクチャ選択を左右
- **公式アダプタ**を選ぶことの重要性
- **モノレポ構成**で開発効率UP

### 今後の展開
- 本番環境でのコスト検証
- EventBridge + WebSocketでリアルタイム化
- 感情データの時系列分析機能

---

## Thank you! 🎉

**GitHub**: https://github.com/your-repo/kibi *(予定)*

**質問・ディスカッション歓迎！**

---

## 補足: 非同期処理の詳細設計

### ジョブフロー
```
1. POST /diary/:id/analyze
   ↓
2. S3にテキストアップロード
   ↓
3. StartDocumentClassificationJob
   ↓
4. ポーリング（3秒間隔）
   ↓
5. 完了時にS3から結果取得
   ↓
6. DynamoDBに保存 + アイコン生成
```

### エラーハンドリング
- ジョブ失敗時のリトライ機能
- タイムアウト処理（最大10分）
- ユーザーへの適切なエラー通知

---

## 補足: カスタム分類器のトレーニング

### データ準備の工夫
```javascript
// 各感情150サンプル（推奨1000+、最低100）
const emotionTemplates = {
  joy: {
    base: ["I am so happy today!", ...],
    variations: ["feeling absolutely wonderful", ...]
  }
};
```

### 品質向上のポイント
- **多様な表現**: 短文、長文、口語、文語
- **バランス**: 各クラス同じサンプル数
- **クリーンアップ**: 改行削除、カンマ処理

**現在150サンプル/感情 → 将来1000+に拡張予定**

---

## 補足: HTTP API Gateway v2の選択理由

| 項目 | REST API | HTTP API v2 |
|-----|---------|-------------|
| 価格 | $3.50/百万リクエスト | $1.00/百万リクエスト |
| レイテンシ | 標準 | **最大60%低減** |
| Lambda統合 | 複雑 | **シンプル** |
| CORS | 設定複雑 | **簡単設定** |

**HTTP API v2を選んだ理由**
- コスト: 1/3.5に削減
- パフォーマンス: レイテンシ低減
- 開発効率: 設定がシンプル
